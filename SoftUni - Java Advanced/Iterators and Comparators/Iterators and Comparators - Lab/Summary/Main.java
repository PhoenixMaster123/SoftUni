package Summary;

public class Main {
    public static void main(String[] args) {
        // 1. Iterable → make the class to can iterate (implements Iterable<type>)

        // 2. Iterator → private class, which knows how this class to iterate
        // (implements Iterator<type> (hasNext, next)

        // 3. Comparable → make this class to can compare the objects (implements Comparable<type>)
        // (method: compareTo(Object o)

        // 4. Comparator → outside class that knows how this class is to compare two objects
        // (method: compare(Object o1, Object o2)
    }
}
